$ set | less # env variables

$ set -o # modify the parameters that are driving the environmen

$ set +0 # same information but prepared for re-use

$ set -x

$ pwd

$ ls -al

$ help | less

$ ls --help